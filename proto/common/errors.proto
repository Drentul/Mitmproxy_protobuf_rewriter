syntax = "proto2";

package messages.common;

option go_package = "common";

// A message representing general error.
message HttpError {
    // The code is the error's code. It may vary depends on the case.
    required int32 code = 1;
    // The human readable message to render the error.
    optional string message = 2;
}